{
  "name": "mountainshares-backend",
  "version": "1.0.0",
  "description": "MountainShares Backend API",
  "scripts": {
    "build": "mkdir -p public && echo 'API-only project' > public/.gitkeep"
  },
  "dependencies": {}
}
